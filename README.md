# Divya Foods — Stage 1

Scaffolded Vite + React + Tailwind project (Stage 1) using provided assets.

Quick start:

```powershell
npm install
npm run dev
```

The app entry is `src/main.jsx` and styles are in `src/styles/index.css`.
